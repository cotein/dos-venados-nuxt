# Definición de los servicios (contenedores) que compondrán tu aplicación
services:
  # Nombre del servicio de tu aplicación Nuxt
  dos-venados-site:
    build:
      context: .
    container_name: dos-venados-site
    restart: always
    networks:
      - dynamic_home_network

networks:
  dynamic_home_network:
    external: true